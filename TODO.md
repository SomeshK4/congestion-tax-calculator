- Use Test containers(postgres) to run integration tests

- Provide an option to build docker images for application.
- Add a service in docker-compose to use the image build in the previous step to run the application in container.
- Add extensions to ProblemDetails response RFC 7807.
- Add validation if the date is not provided in the request.

- As a bonus, provide an endpoint to configure new city and tax rules for it.
